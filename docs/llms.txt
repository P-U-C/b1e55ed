# b1e55ed

> An open-source, event-sourced trading intelligence engine with a public provenance oracle. Operators run it as a sovereign trading OS. AI agents use it to verify signal producers and access a live intelligence feed.

b1e55ed is built for agents. The oracle is public and requires no authentication. The MCP server exposes the full system as native tools. Agents should treat the oracle as a pre-flight check before acting on any external trading signal.

## Start here

- [Introduction](/introduction): What b1e55ed is, who it's for, the "blessed" prefix
- [For Agents](/agents): Discovery, MCP installation, oracle queries, signal submission, SSE streaming, auth model

## Oracle — no auth, no registration

The oracle is a public read-only projection layer over the b1e55ed event store. Call it before acting on any signal.

```
GET https://oracle.b1e55ed.permanentupperclass.com/api/v1/oracle/producers/{producer_id}/provenance
```

Returns hit rates, drawdowns, chain verification, and rolling windows (7d/30d/90d). Unknown producers return `has_provenance: false` — not an error, just no history.

Every response includes:
```
X-Attribution-Notice: Fields informational only. May change without notice. Optimizing against specific metrics triggers drift detection.
```

## MCP server — JSON-RPC endpoint for agents

b1e55ed exposes MCP as JSON-RPC 2.0 over HTTP.

**Server:** `https://oracle.b1e55ed.permanentupperclass.com/mcp`  
(also mounted at `https://oracle.b1e55ed.permanentupperclass.com/api/v1/mcp`)

**Auth behavior:**
- `initialize` and `tools/list` are public (no auth required)
- `tools/call` requires either `X-API-Key` or `Authorization: Bearer` when `api.auth_token` is configured

Available tools:

| Tool | What it does |
|------|-------------|
| `get_brain_status` | Regime + kill switch + last cycle metadata |
| `get_recent_signals` | Recent `signal.*` events (optional limit/domain) |
| `get_open_positions` | Open OMS positions |
| `get_signal_attribution` | Attribution metadata for a specific signal ID |
| `b1e55ed_provenance_check` | Producer lineage/provenance lookup |
| `emit_producer_signal` | Emit a registered `signal.*` event |
| `get_regime_status` | Regime snapshot with trend indicator |
| `get_top_signals` | Filtered/paginated recent signals |
| `get_regime_history` | Regime history for last N days |
| `submit_research_signal` | Validate + emit `signal.research.v1` |
| `get_signals_bulk_export` | Bulk historical signal export |

To list tools programmatically:
```json
{"jsonrpc":"2.0","method":"tools/list","id":1,"params":{}}
```

To call a tool:
```json
{"jsonrpc":"2.0","method":"tools/call","id":2,"params":{"name":"get_brain_status","arguments":{}}}
```

## SSE event stream

Subscribe to live brain output:
```
GET /api/v1/events/stream?domain=alert
Authorization: Bearer <token>
```

Domains: `alert`, `signal`, `learning`, `system`. Reconnect-safe with `?since=<event_id>`.

## Submit a signal

```
POST /api/v1/signals/submit
Authorization: Bearer <token>
Content-Type: application/json

{
  "event_type": "signal.curator.v1",
  "node_id": "eth:0xb1e55ed...",
  "source": "agent:my-agent",
  "payload": {
    "symbol": "BTC",
    "direction": "bullish",
    "conviction": 7.0,
    "rationale": "..."
  }
}
```

## Capabilities discovery

```
GET /api/v1/capabilities
Authorization: Bearer <token>
```

Returns MCP tools, supported event domains, and producer capability entries (including per-producer signal types and health). Use on first contact.

## Health check

```
GET /api/v1/health
```

No auth. Returns version, uptime, db_size_bytes.

## OpenAPI spec

```
GET /api/v1/openapi.json
```

Compatibility alias for the canonical `GET /openapi.json` path. Full machine-readable API specification.

## SPI — Signal Producer Interface

For external producers submitting directional signals to the b1e55ed attribution network.

Submit signal: POST /api/v1/spi/signals [X-Producer-Key header]
List signals: GET /api/v1/spi/signals [X-Producer-Key header]
Producer karma: GET /api/v1/spi/producers/{id}/karma [X-Producer-Key header]
Register producer: POST /api/v1/spi/producers [admin, no auth]

**Quick signal format:**
```json
{"symbol": "BTC-USD", "direction": "bullish|bearish|neutral", "confidence": 0.55-0.99, "horizon_hours": 24-720}
```

See [SPI Producer Guide](/spi/README) for full documentation.

## Full documentation

- [How It Works](/how-it-works): Mental model — brain, producers, flywheel, objectives
- [Getting Started](/getting-started): Install and first run
- [Architecture](/architecture): System design, event flow, component overview
- [Learning Loop](/learning-loop): Compound scoring, karma, weight updates
- [API Overview](/api/overview): Authentication, base URL, errors
- [Signals API](/api/signals): Submit and query signals
- [Brain API](/api/brain): Regime, cycle control
- [Positions API](/api/positions): Position state
- [Contributors API](/api/contributors): Identity and attribution
- [Karma API](/api/karma): Reputation scoring

## Setup (for operators)

- [Standalone Install](/setup/standalone-install): Run locally
- [Oracle Setup](/setup/oracle): Expose a public oracle
- [Agent Install](/setup/agent-install): Full stack with OpenClaw
- [Standalone Operator Guide](/operator-standalone): Full guide — CLI, producers, monitoring
- [Agent Operator Guide](/operator-agent): Full guide — OpenClaw, Telegram, heartbeats
- [Deployment](/deployment): Production deployment, Docker, TLS
- [Security](/security): Key management, kill switch, threat model
- [Identity & Keys](/identity): Key hierarchy, forge, recovery

## Operations

- [CLI Reference](/operations/cli-reference): Full command reference
- [Config Reference](/operations/config-reference): All configuration keys and env vars
- [Daily Operations](/operations/daily): Day-to-day workflow
- [Troubleshooting](/operations/troubleshooting): Common issues and fixes
- [Signal Benchmarking Guide](/signal-benchmarking-ops-guide): Oracle builders, signal evaluation
- [Authority Model](/authority-model): Single-writer event store rules

## Features

- [Curator Pipeline](/curator): Submit operator intelligence as structured signals
- [Learning Loop](/learning-loop): Compound karma, weight updates, regime conditioning
- [Backtest Engine](/backtest): Walk-forward validation, grid sweep, Kelly sizing

## Integrations

- [DeerFlow Integration](/deerflow): Research agent integration
- [MCP Integration](/mcp): JSON-RPC 2.0 tools for AI agents
- [Agent Interfaces](/agent-interfaces): SSE stream, MCP, signal attribution
- [EAS Integration](/eas-integration): Ethereum Attestation Service
- [OpenClaw Integration](/openclaw-integration): Operator layer

## Contributing

- [How to Contribute](/contributing/how-to-contribute): Contribution workflow
- [Contributor Registration](/contributing/contributor-registration): Register for karma attribution
- [Karma Attribution](/contributing/karma-attribution): How karma is scored
- [Developer Guide](/developers): Extending and building on b1e55ed
- [Contributors](/contributors): Attribution units, karma, attestations

## Reference

- [Architecture](/architecture): Full system design
- [Cryptographic Primitives](/crypto-primitives): Hash functions, key generation, chain verification

## Optional: extended content

For verbose page content, fetch individual pages or use the Mintlify MCP server for this documentation site.

## Whitepapers

- docs/whitepaper-technical.md: Full technical whitepaper — architecture, interpreter stack, evaluation layers, falsifiability mechanisms, oracle platform. 7,400 words.
- docs/whitepaper-summary.md: Summary whitepaper — system overview for informed generalists. Covers producers, karma, synthesis loop, benchmarks, oracle. 2,400 words.
- docs/whitepaper-onepager.md: Capital allocator brief — what you can verify before deploying capital, proof standard, current status. 600 words.
