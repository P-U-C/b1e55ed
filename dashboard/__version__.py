# Dashboard versioning — independent of engine version.
# Version 1. Like block 0. Everything after this is downstream.
# Dashboard versioning — independent of engine version.
# Bump this when shipping a new dashboard release (v2, v3, etc.)
# Git tag convention: dashboard/vX.Y.Z

VERSION = "1.0.0"
